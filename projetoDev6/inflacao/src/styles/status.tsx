import { StyleSheet } from 'react-native'


const statusEstilos = StyleSheet.create({
   seta:{
        width: '13%',
        fontSize: 15,
        textAlign: 'center',
        alignItems: 'center',
   }
});

export default statusEstilos